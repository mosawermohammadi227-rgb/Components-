# Components

Reusable animated UI templates for AI products.

## Current template

- AIModelCard — animated, responsive AI model card

## Local test

```bash
npm install
npm run dev
```

The `src/components` directory contains the reusable templates. The playground in `src/App.tsx` is only for testing the current component.